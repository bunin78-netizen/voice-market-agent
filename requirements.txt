# основные зависимости
requests>=2.31
python-telegram-bot>=21.0
python-dotenv>=1.0
matplotlib>=3.7
Pillow>=10.0

# внешние утилиты (не python-пакеты)
#   ffmpeg  — конвертация голосовых OGG/Opus → WAV 16 kHz и сборка opus для ответа
